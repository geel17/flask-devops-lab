# Flask DevOps Lab

## Usage
To run this application locally, activate the virtual environment, install the dependencies, and start the server:

```bash
source .venv/bin/activate
pip install -r requirements.txt
python app.py
